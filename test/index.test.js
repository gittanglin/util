var index = require("../src/index");
//import index from '../src/index'
//var index = require('./../dist/index.min')
var expect = require('chai').expect;




describe("测试", function () {
    it("断言", function () {
        console.log(index)
        //console.log(index.default.TimeUtil.timestampToFormat(new Date().getTime()))
        //expect(index.default.TimeUtil).to.be.a('object');
    });
});
